# Prompt for Codex

You are working in the public GitHub repository `CareerOS`.

Read these files first:

- README.md
- PRD.md
- PROJECT_PLAN.md
- DATABASE_SCHEMA.md
- AGENTS.md
- SEED_PROFILES.json

Task:

1. Initialise a Next.js application using TypeScript and Tailwind CSS.
2. Use the App Router.
3. Create a clean responsive application shell.
4. Add routes:
   - /
   - /profiles
   - /jobs
   - /companies
   - /applications
   - /postgraduate
   - /roadmap
   - /settings
5. Add a left navigation on desktop and a compact mobile navigation.
6. Create typed mock data from `SEED_PROFILES.json`.
7. Build the first dashboard with:
   - active profile selector
   - current goals
   - application status summary
   - upcoming deadlines
   - recommended next actions
8. Do not add authentication or AI APIs yet.
9. Add a clear README section explaining how to run the app.
10. Commit the work with a descriptive commit message.

Quality requirements:

- no `any` types
- accessible buttons and navigation
- loading and empty-state components
- reusable card components
- no unnecessary dependencies
- no secrets committed
