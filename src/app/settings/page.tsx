import { FeaturePage } from "@/components/pages/feature-page";
export default function SettingsPage() {
  return <FeaturePage eyebrow="Workspace" title="Settings" description="Manage preferences for your CareerOS workspace." emptyIcon="⚙" emptyTitle="No settings to configure yet" emptyDescription="Account and notification preferences will be added after the core career workflows are complete." />;
}
